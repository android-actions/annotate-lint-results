name: 'Annotate Android Linting'
description: 'Submit lint results as github annotations'
author: 'Android-Actions'

inputs:
  token:
    required: true
    description: 'token to use for submitting'

runs:
  using: 'node12'
  main: 'dist/index.js'
